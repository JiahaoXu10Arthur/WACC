package wacc

import org.scalatest._
import flatspec._
import matchers._

class ParserExprTest {
    
    "int liter" should "be parsed" in pending
    
    "bool liter" should "be parsed" in pending

    "char liter" should "be parsed" in pending

    "string liter" should "be parsed" in pending

    "pair liter" should "be parsed" in pending

    "identifier" should "be parsed" in pending

    "array element" should "be parsed" in pending

    "unary operator <expr>" should "be parsed" in pending

    "<expr> binary operator <expr>" should "be parsed" in pending

    "( <expr> )" should "be parsed" in pending

}

