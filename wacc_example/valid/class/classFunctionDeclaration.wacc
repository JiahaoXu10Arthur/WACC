# a simple function with class return type is declared, but not called

# Output:

# Program:

begin
  class foo of
    int field1

    int foo() is
	    return 5
		end
  end

	class foo f() is
		class foo a = {1};
    return a 
  end

  skip
end
