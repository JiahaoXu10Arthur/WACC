# attempted redefinition of function with different parameters

# Output:
# #semantic_error#

# Exit:
# 0
# 1

# Program:

begin
	int f(int a) is
		return 0
	end
  int f(bool a) is
    return 1
  end
  int x = call f(1);
  println x

  int y = call f(true);
  println y
end
