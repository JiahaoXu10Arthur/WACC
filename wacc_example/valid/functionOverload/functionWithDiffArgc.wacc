# attempted overload of function with different parameters numbers

# Output:
# 0
# 1
#

# Program:

begin
	int f(int a) is
		return 0
	end

  int f(int a, char b) is
    return 1
  end

  int x = call f(1);
  println x;
  int y = call f(1, 'a');
  println y
end
